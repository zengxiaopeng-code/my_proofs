/-!
# Results (§3.3): criterion transform — SKELETON

`prop:criterion transform`: every condition of `thm:nogain-delta-eps` can be checked on the static
truncation value `V_t` rather than the continuation value `U_t` (uses `lem:cap-equiv`, optional
sampling of the affine price along the posterior martingale).
-/
