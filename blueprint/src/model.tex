% Blueprint chapter: the Model (§2), decomposed to field level for human paper<->Lean cross-check.
% Role-distinct environments (see macros/common.tex and the Legend in the Lemma 1 chapter):
%   definition = a paper §2 notion (title cites the paper section);
%   faithcheck = a machine-checked faithfulness audit of the encoding (NOT paper math).
% There are NO `lemma` nodes here: §2 contributes definitions + faithfulness checks only.
% Verbatim paper text lives in docs/paper-model.md (single source of truth).
% Each node states a faithfulness tag: [faithful] transcribed as-is / [weakening] Lean hypothesis
% is weaker than the paper (safe) / [encoding] a modelling choice whose transparency is a green check.

\chapter{Model (\S2): the dynamic mechanism game}

This chapter formalizes the paper's \textbf{model} (\S2): the belief space, the allocation date,
and the reduced-form continuation/truncation values with the single-shot stopping recursion.
\textbf{The verbatim paper text is in \texttt{docs/paper-model.md}}; this chapter decomposes it to
\emph{field level} so each Lean field can be checked against one clause of the paper.

\noindent\textbf{Node kinds here} (see the full Legend in the Lemma~1 chapter): the
\textbf{Definition} nodes are paper \S2 notions (titles cite the section); the
\textbf{Faithfulness check} nodes are machine-checked audits that the Lean encoding matches the
paper --- they are \emph{not} paper lemmas and carry their own counter. Following the paper's own
choice (Assumption~2, analytic equilibrium), the dynamic game is an \textbf{abstract
axiomatization}: equilibrium continuation values are carried as data and the game-theoretic
assumptions are hypotheses. So \S2 is almost all \emph{definitions}, which the kernel cannot vet for
faithfulness on its own; the bracketed tag on each node
([faithful]/[weakening]/[encoding]) says what to check, and the Faithfulness-check nodes turn as
much of that as possible into green, kernel-checked facts (mirrors \texttt{Meta/ModelFaithfulness.lean}).

\noindent\textbf{Reminder:} green $=$ the kernel checked the proof against the \emph{stated} Lean
proposition. It is \emph{not} a claim that the proposition matches the paper --- that is what the
bracketed tags and the Faithfulness-check nodes speak to. See the boxed note in the Lemma~1 chapter.

%======================================================================
% §2.1 Belief space and allocation date
%======================================================================
\begin{definition}[Belief space $\Delta(\Theta)$ (paper \S2.1)]\label{def:belief}
    \lean{DMC.Belief}\leanok
    \uses{def:setup}
    The belief space $\Delta(\Theta)$ is the set of Borel probability measures on the compact
    Polish state space $\Theta$, with the weak topology. In Lean, \texttt{Belief}~$\Theta$ is
    \texttt{ProbabilityMeasure}~$\Theta$. \emph{[faithful]} As established in
    Step~\ref{lem:deltaTheta-sb}, it is a compact standard Borel space.

% BEGIN-LEAN-STMT (auto-generated by tools/gen_lean_statements.py -- do not edit)
\par\smallskip\noindent\textbf{Lean statement (printed by Lean; verbatim):}
\begin{verbatim}
DMC.Belief : (Θ : Type u_1) → [MeasurableSpace Θ] → Type u_1
\end{verbatim}
% END-LEAN-STMT
\end{definition}

\begin{definition}[Allocation date $\tau$ (paper \S2.1, Timeline)]\label{def:allocdate}
    \lean{DMC.AllocDate}\leanok
    The allocation date is $\tau\in\{1,2,\dots\}\cup\{\emptyset\}$, where $\emptyset$ means ``no
    sale ever occurs''. \emph{[encoding]} In Lean it is
    \texttt{Option \{n : $\mathbb N$ // 1 $\le$ n\}}, with \texttt{none}$=\emptyset$ --- the dates
    carry the paper's constraint $\tau\ge 1$, so no spurious date $0$ is representable. Faithfulness
    of this encoding is Faithfulness check~\ref{fc:allocdate}.

% BEGIN-PAPER-STMT (auto-generated by tools/gen_paper_statements.py -- do not edit)
\par\smallskip\noindent\textbf{Paper text (verbatim from the manuscript source):}
\begin{verbatim}
\paragraph{Timeline.} Let $\tau\in\{1,2,...\}\cup\{\varnothing\}$ denote the date at which the resource is actually allocated. If $\tau=\varnothing$, no sale ever occurs.At each date $t$ and after any public state $Z_t$, the seller chooses, and
commits to, a continuation mechanism $\Gamma_t\in\mathcal H_t$, but cannot
commit to the mechanisms of later dates.\footnote{Formally, $\mathcal H_t$ contains no mechanism that binds later
choices. The calendar accordingly arises date by date. The seller chooses
$\Gamma_2$ only when date $1$ has ended and $Z_2$ has realized, $\Gamma_3$
only when date $2$ has ended, and so on. In $R(\mathcal M)$, the calendar
is the realized record of the game, not a plan fixed in advance.} is a recording by time: $\mathcal M=\{(Z_0,\Gamma_0), (Z_1,\Gamma_1),\dots\}$. Let \(R(\mathcal M)\) denote the supremum of the seller's ex-ante payoff over all sequentially rational Bayesian equilibria of the dynamic game associated with the calendar $\mathcal M$.
\end{verbatim}
% END-PAPER-STMT
% BEGIN-LEAN-STMT (auto-generated by tools/gen_lean_statements.py -- do not edit)
\par\smallskip\noindent\textbf{Lean statement (printed by Lean; verbatim):}
\begin{verbatim}
DMC.AllocDate : Type
\end{verbatim}
% END-LEAN-STMT
\end{definition}

\begin{faithcheck}[$\emptyset$ is a genuine ``never''; dates are preserved]\label{fc:allocdate}
    \lean{DMC.allocDate_never_ne_date, DMC.allocDate_date_injective,
      DMC.allocDate_date_ge_one}\leanok
    \uses{def:allocdate}
    The ``never'' symbol \texttt{none} is distinct from every real date \texttt{some}~$n$; distinct
    dates encode to distinct values; and every encoded date satisfies $\tau\ge1$. So the encoding
    loses no information, never identifies $\emptyset$ with a date, and admits no value outside the
    paper's range $\{1,2,\dots\}\cup\{\emptyset\}$.

% BEGIN-LEAN-STMT (auto-generated by tools/gen_lean_statements.py -- do not edit)
\par\smallskip\noindent\textbf{Lean statements (printed by Lean; verbatim):}
\begin{verbatim}
DMC.allocDate_never_ne_date : ∀ (n : { n // 1 ≤ n }), none ≠ some n
\end{verbatim}
\begin{verbatim}
DMC.allocDate_date_injective : Function.Injective some
\end{verbatim}
\begin{verbatim}
DMC.allocDate_date_ge_one : ∀ (n : { n // 1 ≤ n }), 1 ≤ ↑n
\end{verbatim}
% END-LEAN-STMT
\end{faithcheck}

%======================================================================
% §2.2 Reduced-form values
%======================================================================
\begin{definition}[Date values $U_t,V_t$ --- \emph{interface}]\label{def:datevalues}
    \lean{DMC.DateValues}\leanok
    \uses{def:belief}
    \textbf{This is the downstream \emph{interface}, not the paper's definition.} The paper
    \emph{defines} $U_t$ and $V_t$ as suprema over mechanism rules and payoff selections
    (Definitions~\ref{def:contvalue} and~\ref{def:truncvalue}); \texttt{DateValues} only records the
    properties \S3 consumes. It is now \emph{constructed} from those definitions by
    \texttt{toDateValues} (Faithfulness check~\ref{fc:datevalues-derived}), so the fields below are
    no longer assumptions. In Lean it bundles $U,V:\mathbb N\to\Delta(\Theta)\to\mathbb R$ with:
    \begin{itemize}
      \item \texttt{V\_nonneg}: $V_t\ge 0$ --- the absorbing no-sale node pays $0$; now
        \emph{derived} (Faithfulness check~\ref{fc:vnonneg});
      \item \texttt{V\_le\_U}: $V_t\le U_t$ \emph{[weakening]} --- deferring is an option, so this is
        a genuine ``$\le$'', not an equality (witnessed by Faithfulness check~\ref{fc:vltu});
      \item \texttt{U\_bddAbove}: $U$ is uniformly bounded above --- Assumption~2(iv), equilibrium
        values are finite; now \emph{derived} (Faithfulness check~\ref{fc:ubdd}).
    \end{itemize}

% BEGIN-LEAN-STMT (auto-generated by tools/gen_lean_statements.py -- do not edit)
\par\smallskip\noindent\textbf{Lean statement (printed by Lean; verbatim):}
\begin{verbatim}
DMC.DateValues : (Θ : Type u_1) → [MeasurableSpace Θ] → Type u_1
\end{verbatim}
% END-LEAN-STMT
\end{definition}

\begin{definition}[Date-wise envelope $g=\sup_t U_t$ (paper \S2.2)]\label{def:envelope}
    \lean{DMC.DateValues.envelope}\leanok
    \uses{def:datevalues}
    The paper's upper envelope $g(\mu):=\sup_t U_t(\mu)$ (resp.\ $\hat g$ for a richer
    conditioning). \emph{[faithful]} Transparency: this really is the supremum
    (Faithfulness check~\ref{fc:envelope}); a richer conditioning weakly raises it,
    $g\le\hat g$ (\texttt{DMC.DateValues.envelope\_mono}).

% BEGIN-LEAN-STMT (auto-generated by tools/gen_lean_statements.py -- do not edit)
\par\smallskip\noindent\textbf{Lean statement (printed by Lean; verbatim):}
\begin{verbatim}
@DMC.DateValues.envelope : {Θ : Type u_1} →
  [inst : MeasurableSpace Θ] → DMC.DateValues Θ → MeasureTheory.ProbabilityMeasure Θ → ℝ
\end{verbatim}
% END-LEAN-STMT
\end{definition}

\begin{definition}[Single-shot structure --- \emph{interface}]\label{def:singleshot}
    \lean{DMC.SingleShot}\leanok
    \uses{def:datevalues, lem:posterior-martingale}
    A \texttt{DateValues} together with the belief-martingale conditional-expectation operator and
    the single-shot stopping recursion. \textbf{It does \emph{not} carry Assumption~1}: the paper's
    Assumption~1 is the economic statement that the resource is allocated at most once, whereas this
    structure carries its \emph{consequence}. Of Assumption~2 only clause~(iv) appears (inherited
    from \texttt{DateValues}). Its fields:
    \begin{itemize}
      \item \texttt{condExp}~$\varphi\,t\,\mu=\mathbb E[\varphi(S_{t+1})\mid S_t=\mu]$, the expected
        next-date value of a belief functional \emph{[encoding]};
      \item \texttt{condExp\_mono}: it is a positive (monotone) Markov operator \emph{[faithful]};
      \item \texttt{affine\_harmonic}: affine functionals $\nu\mapsto\int\lambda\,d\nu$ are harmonic
        (Bayes-plausibility) \emph{[faithful]} --- the operative form of the posterior-martingale
        property (Step~\ref{lem:martingale});
      \item \texttt{stopping}: $U_t(\mu)=\max\{V_t(\mu),\,\mathbb E[U_{t+1}\mid\mu]\}$
        \emph{[strengthening]} --- allocate now or defer, whichever is larger. \textbf{The paper
        \emph{derives} this} (\texttt{eq:stopping-form}: the continuation value is the Snell envelope
        of the truncation values, by \texttt{lem:stopping-form} from Assumption~1); here it is
        assumed. It is the \emph{only} remaining assumed item of the model layer. The $\max$ really
        is the stop-or-continue decision (Faithfulness check~\ref{fc:stop}).
    \end{itemize}

% BEGIN-PAPER-STMT (auto-generated by tools/gen_paper_statements.py -- do not edit)
\par\smallskip\noindent\textbf{Paper text (verbatim from the manuscript source):}
\begin{verbatim}
\begin{assumption}[Single-shot resource]\label{ass:single-shot}
The resource is economically single-shot: it can be allocated at most once. If the resource is assigned to some buyers and transfers are made, then no later date $t'>t$ can generate additional surplus.\footnote{If Assumption \ref{ass:single-shot} does not hold, the problem may degenerate into a simple problem of finding the pointwise optimal continuation mechanism at each date $t$, the intertemporal problem loses the single-shot structure that motivates our analysis.}
\end{assumption}
\end{verbatim}
% END-PAPER-STMT
% BEGIN-LEAN-STMT (auto-generated by tools/gen_lean_statements.py -- do not edit)
\par\smallskip\noindent\textbf{Lean statement (printed by Lean; verbatim):}
\begin{verbatim}
DMC.SingleShot : (Θ : Type u_1) → [TopologicalSpace Θ] → [MeasurableSpace Θ] → Type u_1
\end{verbatim}
% END-LEAN-STMT
\end{definition}

%======================================================================
% §2.2 primitives: U_t and V_t as DEFINITIONS (Model/Primitives.lean)
%======================================================================
\section*{The paper's primitives: $U_t$ and $V_t$ as definitions}

The paper does not postulate $U_t$; it \emph{defines} it as a supremum over continuation mechanism
rules and payoff selections. It also never constructs the strategy--belief profiles $\mathcal B_t$,
the sequentially rational Bayesian equilibria $\mathcal E_t$, or the payoff $\pi_t$: it constrains
them by Assumption~2 and works with the induced payoff correspondence $\mathcal R_t^{\Gamma_t}$.
The nodes below put the Lean abstraction boundary \textbf{at the same place}, so that $U_t$, $V_t$
and the monotonicity in $\mathcal G$ become definitions and theorems rather than axioms.

\begin{definition}[Payoff selections $\Sel(\mathcal R_t^{\Gamma_t})$ (paper \S2.2)]\label{def:selections}
    \lean{DMC.Paper.Sel}\leanok
    For a continuation mechanism rule $\Gamma_t$, the set of measurable payoff selections of the
    induced payoff correspondence $\mathcal R_t^{\Gamma_t}$. \emph{[deviation]} The paper uses
    \emph{universally} measurable selections; Lean uses Borel measurable ones, which shrinks the
    feasible set and hence makes the $U_t$ below $\le$ the paper's.

% BEGIN-PAPER-STMT (auto-generated by tools/gen_paper_statements.py -- do not edit)
\par\smallskip\noindent\textbf{Paper text (verbatim from the manuscript source):}
\begin{verbatim}
\paragraph{Continuation value.} For a continuation mechanism $\Gamma$, let
$\Sel(\mathcal R_t^{\Gamma_t})$ denote the set of universally measurable payoff selections of $\mathcal R_t^{\Gamma_t}$ (see \cref{lem:selection-exists}). Public history $Z_t$ induces a posterior $S_t:\Omega\to\Delta(\Theta)$ which summarizes all public learning about the primitive state $\theta$ (see \cref{lem:posterior-martingale}). Let $\mathbb P_t^\mu(\cdot)$ be a version of $\mathbb P(\cdot\mid S_t(\omega)=\mu)$, which denotes the conditional law of the state $\omega$ given the public posterior $\mu$. \emph{Conditioning on} the public continuation state \(Z_t\), the date-\(t\) \emph{continuation value} is\footnote{the definition is expectation-based: the object of interest is the seller's date-$t$ conditional expected equilibrium payoff at a given posterior, not a pathwise conditional essential supremum over value. Whenever a continuation value $U_t^{\mathcal G}$ admits multiple measurable versions, we fix once and for all a bounded upper semianalytic version and continue to denote it by the same symbol.}
\end{verbatim}
% END-PAPER-STMT
% BEGIN-LEAN-STMT (auto-generated by tools/gen_lean_statements.py -- do not edit)
\par\smallskip\noindent\textbf{Lean statement (printed by Lean; verbatim):}
\begin{verbatim}
@DMC.Paper.Sel : {Ω : Type u_1} →
  [MeasurableSpace Ω] → {H : ℕ → Type u_2} → ((t : ℕ) → (Ω → H t) → Ω → Set ℝ) → (t : ℕ) → (Ω → H t) → Set (Ω → ℝ)
\end{verbatim}
% END-LEAN-STMT
\end{definition}

\begin{definition}[Continuation value $U_t^{\mathcal G}$ (paper \S2.2)]\label{def:contvalue}
    \lean{DMC.Paper.U}\leanok
    \uses{def:belief, def:selections}
    \emph{[faithful]} The paper's definition, transcribed:
    \[
      U_t^{\mathcal G}(\mu)\;:=\;\sup\Bigl\{\int_\Omega \rho\,d\mathbb P_t^\mu \;:\;
        \Gamma\text{ is }\mathcal G\text{-measurable},\ \rho\in\Sel(\mathcal R_t^{\Gamma})\Bigr\}.
    \]
    The payoff correspondence $\mathcal R_t^{\Gamma}$ and the conditional law $\mathbb P_t^\mu$ are
    parameters, exactly as in the paper.

% BEGIN-PAPER-STMT (auto-generated by tools/gen_paper_statements.py -- do not edit)
\par\smallskip\noindent\textbf{Paper text (verbatim from the manuscript source):}
\begin{verbatim}
\paragraph{Continuation value.} For a continuation mechanism $\Gamma$, let
$\Sel(\mathcal R_t^{\Gamma_t})$ denote the set of universally measurable payoff selections of $\mathcal R_t^{\Gamma_t}$ (see \cref{lem:selection-exists}). Public history $Z_t$ induces a posterior $S_t:\Omega\to\Delta(\Theta)$ which summarizes all public learning about the primitive state $\theta$ (see \cref{lem:posterior-martingale}). Let $\mathbb P_t^\mu(\cdot)$ be a version of $\mathbb P(\cdot\mid S_t(\omega)=\mu)$, which denotes the conditional law of the state $\omega$ given the public posterior $\mu$. \emph{Conditioning on} the public continuation state \(Z_t\), the date-\(t\) \emph{continuation value} is\footnote{the definition is expectation-based: the object of interest is the seller's date-$t$ conditional expected equilibrium payoff at a given posterior, not a pathwise conditional essential supremum over value. Whenever a continuation value $U_t^{\mathcal G}$ admits multiple measurable versions, we fix once and for all a bounded upper semianalytic version and continue to denote it by the same symbol.}
\[
U_t^{\sigma(Z_t)}(\mu)
:=
\sup\Bigg\{
\int_\Omega \rho(\omega)\,\mathbb P_t^\mu(d\omega):
\Gamma \text{ is } \sigma(Z_t)\text{-measurable},
\
\rho\in \Sel\big(\mathcal R_t^{\Gamma_t}\big)
\Bigg\},
\qquad
\mu\in\Delta(\Theta).
\]
\end{verbatim}
% END-PAPER-STMT
% BEGIN-LEAN-STMT (auto-generated by tools/gen_lean_statements.py -- do not edit)
\par\smallskip\noindent\textbf{Lean statement (printed by Lean; verbatim):}
\begin{verbatim}
@DMC.Paper.U : {Ω : Type u_1} →
  {Θ : Type u_2} →
    [inst : MeasurableSpace Ω] →
      [inst_1 : MeasurableSpace Θ] →
        {H : ℕ → Type u_3} →
          ((t : ℕ) → MeasurableSpace (H t)) →
            ((t : ℕ) → (Ω → H t) → Ω → Set ℝ) →
              (ℕ → MeasureTheory.ProbabilityMeasure Θ → MeasureTheory.Measure Ω) →
                DMC.Paper.Richness Ω → ℕ → MeasureTheory.ProbabilityMeasure Θ → ℝ
\end{verbatim}
% END-LEAN-STMT
\end{definition}

\begin{definition}[Truncation value $V_t^{\mathcal G}$ (paper \S3.3)]\label{def:truncvalue}
    \lean{DMC.Paper.V}\leanok
    \uses{def:contvalue}
    \emph{[faithful]} The same supremum, over the \emph{truncation} payoff correspondence
    $\mathcal V_t^{\Gamma}$ (every public continuation replaced by the absorbing no-sale node).

% BEGIN-PAPER-STMT (auto-generated by tools/gen_paper_statements.py -- do not edit)
\par\smallskip\noindent\textbf{Paper text (verbatim from the manuscript source):}
\begin{verbatim}
\paragraph{Truncation value.}
Fix $t\in\mathbb T$, a public state $z$, and a continuation mechanism $h\in\mathcal H_t$. The \emph{truncation} of the date-$t$ subgame $(z,h)$ replaces every public continuation history at which $Z_{t+1}$ realizes with the absorbing no-sale node, which yields the seller payoff $0$ and assigns each buyer the reservation payoff $0$; let $\mathcal E_t^{\circ}(z,h)$ denote its set of sequentially rational strategy-belief profiles. For an $\sigma(Z_t)$-measurable continuation rule $\Gamma_t$, the \emph{truncation payoff correspondence} is
\[
\mathcal V_t^{\Gamma_t}(\omega)
:=
\bigl\{
\pi_t\bigl(\omega,Z_t(\omega),\Gamma_t(\omega),b\bigr):
b\in\mathcal E_t^{\circ}\bigl(Z_t(\omega),\Gamma_t(\omega)\bigr)
\bigr\},
\]
and the \emph{truncation value} conditioning on a public state $\mathcal G_t$ with $\sigma(S_t)\subseteq\mathcal G_t\subseteq\sigma(Z_t)$ is
\[
V_t^{\mathcal G_t}(\mu)
:=
\sup\Bigl\{
\int_\Omega \rho(\omega)\,\mathbb P_t^\mu(d\omega):
\Gamma_t\text{ is }\mathcal G_t\text{-measurable},\
\rho\in\Sel\bigl(\mathcal V_t^{\Gamma_t}\bigr)
\Bigr\},
\qquad
\mu\in\Delta(\Theta).
\]
\end{verbatim}
% END-PAPER-STMT
% BEGIN-LEAN-STMT (auto-generated by tools/gen_lean_statements.py -- do not edit)
\par\smallskip\noindent\textbf{Lean statement (printed by Lean; verbatim):}
\begin{verbatim}
@DMC.Paper.V : {Ω : Type u_1} →
  {Θ : Type u_2} →
    [inst : MeasurableSpace Ω] →
      [inst_1 : MeasurableSpace Θ] →
        {H : ℕ → Type u_3} →
          ((t : ℕ) → MeasurableSpace (H t)) →
            ((t : ℕ) → (Ω → H t) → Ω → Set ℝ) →
              (ℕ → MeasureTheory.ProbabilityMeasure Θ → MeasureTheory.Measure Ω) →
                DMC.Paper.Richness Ω → ℕ → MeasureTheory.ProbabilityMeasure Θ → ℝ
\end{verbatim}
% END-LEAN-STMT
\end{definition}

\begin{definition}[Admissible richness and collapse (paper \S2.3)]\label{def:collapse}
    \lean{DMC.Paper.AdmissibleRichness, DMC.Paper.ValueCollapse}\leanok
    \uses{def:contvalue}
    \emph{[faithful]} The paper's state decomposition $Z_t=(S_t,Y_t)$ and the sandwich
    $\sigma(S_t)\subseteq\mathcal G\subseteq\sigma(S_t,Y_t)$, expressed with the order on
    \texttt{MeasurableSpace}~$\Omega$; and \emph{continuation value collapse}
    $U_t^{\sigma(S_t,Y_t)}(\mu)=U_t^{\mathcal G}(\mu)$ for all $\mu$. Before $U_t$ was a definition,
    none of this was expressible.

% BEGIN-PAPER-STMT (auto-generated by tools/gen_paper_statements.py -- do not edit)
\par\smallskip\noindent\textbf{Paper text (verbatim from the manuscript source):}
\begin{verbatim}
\paragraph{State decomposition.} As mentioned above, the posterior $S_t$ is sufficient for inference about $\theta$. However, two realized public histories may induce the same posterior $S_t$ and still lead to different date-$t$ continuation problems.\footnote{Examples include eligibility states, promised utilities, inventories, reputations, or other public variables that do not further change beliefs about \(\theta\), but may affect the continuation problem.} To capture this distinction, we impose the following decomposition of the public state. Without loss of generality, we rewrite the date-$t$ public state as
\[
Z_t:=(S_t,Y_t),
\]
where $S_t$ is the posterior and $Y_t$ denotes the non-posterior public continuation state.\footnote{Non-posterior public state $Y_t$ does not change beliefs about $\theta$ beyond what is already encoded in $S_t$, but it may remain payoff-relevant for the induced date-$t$ continuation mechanism problem. We provided a specific definition (\cref{eq:Non-posterior}) in \cref{sec:screening-application}.} Posterior $S_t$ summarizes public learning about the primitive state, whereas $(S_t,Y_t)$ is the full public state relevant for the date-$t$ mechanism problem. The state decomposition allows us to compare different public states at date $t$. Let $\mathcal G$ be a public state satisfying
\[
\sigma(S_t)\subseteq \mathcal G\subseteq \sigma(S_t,Y_t).
\]
That is, $\mathcal G$ summarizes the public states on which the seller is allowed to condition the date-$t$ continuation mechanism.
\end{verbatim}
\begin{verbatim}
Given public state $\sigma(S_t)\subseteq \mathcal G\subseteq \sigma(S_t,Y_t)$ and $Y_t$, we say that the selector collapse holds from \(\sigma(S_t,Y_t)\) to \(\mathcal G\) if, for every \(\mu\), there exists a \(\sigma(S_t,Y_t)\)-optimal selector \((\Gamma_t,\rho)\) at \(\mu\) such that \(\Gamma_t\) is \(\mathcal G\)-measurable; and the continuation value collapse from \(\sigma(S_t,Y_t)\) to \(\mathcal G\) if
\[
U_t^{\sigma(S_t,Y_t)}(\mu)=U_t^{\mathcal G}(\mu)
\qquad
\forall\,\mu\in\Delta(\Theta).
\]
\end{verbatim}
% END-PAPER-STMT
% BEGIN-LEAN-STMT (auto-generated by tools/gen_lean_statements.py -- do not edit)
\par\smallskip\noindent\textbf{Lean statements (printed by Lean; verbatim):}
\begin{verbatim}
@DMC.Paper.AdmissibleRichness : {Ω : Type u_1} →
  {Θ : Type u_2} →
    [inst : MeasurableSpace Θ] →
      {𝓨 : Type u_3} →
        [MeasurableSpace 𝓨] → (Ω → MeasureTheory.ProbabilityMeasure Θ) → (Ω → 𝓨) → DMC.Paper.Richness Ω → Prop
\end{verbatim}
\begin{verbatim}
@DMC.Paper.ValueCollapse : {Ω : Type u_1} →
  {Θ : Type u_2} →
    [inst : MeasurableSpace Ω] →
      [inst_1 : MeasurableSpace Θ] →
        {H : ℕ → Type u_3} →
          ((t : ℕ) → MeasurableSpace (H t)) →
            ((t : ℕ) → (Ω → H t) → Ω → Set ℝ) →
              (ℕ → MeasureTheory.ProbabilityMeasure Θ → MeasureTheory.Measure Ω) →
                {𝓨 : Type u_4} →
                  [MeasurableSpace 𝓨] →
                    (Ω → MeasureTheory.ProbabilityMeasure Θ) → (Ω → 𝓨) → DMC.Paper.Richness Ω → ℕ → Prop
\end{verbatim}
% END-LEAN-STMT
\end{definition}

\begin{faithcheck}[Richer conditioning raises $U_t$ --- now a \emph{theorem}]\label{fc:umono}
    \lean{DMC.Paper.U_mono_of_le}\leanok
    \uses{def:contvalue}
    The paper's ``allowing richer public states expands each date-$t$ continuation value $U_t$'' is
    proved: $\mathcal G_1\subseteq\mathcal G_2$ admits more mechanism rules, so the supremum runs
    over a larger set. \textbf{This was previously a bare hypothesis} (the argument of
    \texttt{DateValues.envelope\_mono}) --- the largest faithfulness hole in the model layer.

% BEGIN-LEAN-STMT (auto-generated by tools/gen_lean_statements.py -- do not edit)
\par\smallskip\noindent\textbf{Lean statement (printed by Lean; verbatim):}
\begin{verbatim}
@DMC.Paper.U_mono_of_le : ∀ {Ω : Type u_1} {Θ : Type u_2} [inst : MeasurableSpace Ω] [inst_1 : MeasurableSpace Θ]
  {H : ℕ → Type u_3} (mH : (t : ℕ) → MeasurableSpace (H t)) (R : (t : ℕ) → (Ω → H t) → Ω → Set ℝ)
  (Pcond : ℕ → MeasureTheory.ProbabilityMeasure Θ → MeasureTheory.Measure Ω) [∀ (t : ℕ), Nonempty (H t)]
  [∀ (t : ℕ) (μ : MeasureTheory.ProbabilityMeasure Θ), MeasureTheory.IsProbabilityMeasure (Pcond t μ)] {M : ℝ},
  (∀ (t : ℕ) (Γ : Ω → H t) (ω : Ω), ∀ r ∈ R t Γ ω, r ≤ M) →
    (∀ (t : ℕ) (Γ : Ω → H t) (μ : MeasureTheory.ProbabilityMeasure Θ),
        ∃ ρ ∈ DMC.Paper.Sel R t Γ, MeasureTheory.Integrable ρ (Pcond t μ)) →
      ∀ {𝒢₁ 𝒢₂ : DMC.Paper.Richness Ω},
        𝒢₁.σ ≤ 𝒢₂.σ →
          ∀ (t : ℕ) (μ : MeasureTheory.ProbabilityMeasure Θ),
            DMC.Paper.U mH R Pcond 𝒢₁ t μ ≤ DMC.Paper.U mH R Pcond 𝒢₂ t μ
\end{verbatim}
% END-LEAN-STMT
\end{faithcheck}

\begin{faithcheck}[$U_t$ bounded --- derived from Assumption~2(iv)]\label{fc:ubdd}
    \lean{DMC.Paper.U_bddAbove_of_bound}\leanok
    \uses{def:contvalue}
    Assumption~2(iv) (``the payoff correspondence is uniformly bounded above: equilibrium values are
    finite'') now \emph{implies} the uniform bound on $U$. The paper assumes boundedness of
    $\mathcal R_t^{\Gamma}$; \texttt{DateValues.U\_bddAbove} assumed it directly on $U$, one level
    too high.

% BEGIN-LEAN-STMT (auto-generated by tools/gen_lean_statements.py -- do not edit)
\par\smallskip\noindent\textbf{Lean statement (printed by Lean; verbatim):}
\begin{verbatim}
@DMC.Paper.U_bddAbove_of_bound : ∀ {Ω : Type u_1} {Θ : Type u_2} [inst : MeasurableSpace Ω] [inst_1 : MeasurableSpace Θ]
  {H : ℕ → Type u_3} (mH : (t : ℕ) → MeasurableSpace (H t)) (R : (t : ℕ) → (Ω → H t) → Ω → Set ℝ)
  (Pcond : ℕ → MeasureTheory.ProbabilityMeasure Θ → MeasureTheory.Measure Ω) [∀ (t : ℕ), Nonempty (H t)]
  [∀ (t : ℕ) (μ : MeasureTheory.ProbabilityMeasure Θ), MeasureTheory.IsProbabilityMeasure (Pcond t μ)] {M : ℝ},
  (∀ (t : ℕ) (Γ : Ω → H t) (ω : Ω), ∀ r ∈ R t Γ ω, r ≤ M) →
    (∀ (t : ℕ) (Γ : Ω → H t) (μ : MeasureTheory.ProbabilityMeasure Θ),
        ∃ ρ ∈ DMC.Paper.Sel R t Γ, MeasureTheory.Integrable ρ (Pcond t μ)) →
      ∀ (𝒢 : DMC.Paper.Richness Ω) (t : ℕ) (μ : MeasureTheory.ProbabilityMeasure Θ), DMC.Paper.U mH R Pcond 𝒢 t μ ≤ M
\end{verbatim}
% END-LEAN-STMT
\end{faithcheck}

\begin{faithcheck}[$V_t\ge 0$ --- derived from the no-sale node]\label{fc:vnonneg}
    \lean{DMC.Paper.V_nonneg}\leanok
    \uses{def:truncvalue}
    The paper says ``\emph{By construction} $V_t^{\mathcal G_t}\ge 0$'': the truncation's absorbing
    no-sale node pays $0$, so the zero selection is always feasible. Assuming that \emph{primitive},
    $V\ge0$ is proved. Previously the axiom field \texttt{DateValues.V\_nonneg}.

% BEGIN-LEAN-STMT (auto-generated by tools/gen_lean_statements.py -- do not edit)
\par\smallskip\noindent\textbf{Lean statement (printed by Lean; verbatim):}
\begin{verbatim}
@DMC.Paper.V_nonneg : ∀ {Ω : Type u_1} {Θ : Type u_2} [inst : MeasurableSpace Ω] [inst_1 : MeasurableSpace Θ]
  {H : ℕ → Type u_3} (mH : (t : ℕ) → MeasurableSpace (H t)) (Rtrunc : (t : ℕ) → (Ω → H t) → Ω → Set ℝ)
  (Pcond : ℕ → MeasureTheory.ProbabilityMeasure Θ → MeasureTheory.Measure Ω) [∀ (t : ℕ), Nonempty (H t)]
  [∀ (t : ℕ) (μ : MeasureTheory.ProbabilityMeasure Θ), MeasureTheory.IsProbabilityMeasure (Pcond t μ)] {M : ℝ},
  (∀ (t : ℕ) (Γ : Ω → H t) (ω : Ω), ∀ r ∈ Rtrunc t Γ ω, r ≤ M) →
    (∀ (t : ℕ) (Γ : Ω → H t) (ω : Ω), 0 ∈ Rtrunc t Γ ω) →
      ∀ (𝒢 : DMC.Paper.Richness Ω) (t : ℕ) (μ : MeasureTheory.ProbabilityMeasure Θ),
        0 ≤ DMC.Paper.V mH Rtrunc Pcond 𝒢 t μ
\end{verbatim}
% END-LEAN-STMT
\end{faithcheck}

\begin{faithcheck}[$V_t\le U_t$ --- derived from the stopping form]\label{fc:vleu-stop}
    \lean{DMC.Paper.V_le_U_of_stopping}\leanok
    \uses{def:truncvalue}
    From \texttt{eq:stopping-form} $U_t=\max\{V_t,\ \mathbb E[U_{t+1}\mid\mu]\}$, the inequality
    $V_t\le U_t$ is immediate. So \texttt{V\_le\_U} is no longer an independent assumption; the
    stopping form is the \emph{single} remaining assumed item (see Definition~\ref{def:singleshot}).

% BEGIN-LEAN-STMT (auto-generated by tools/gen_lean_statements.py -- do not edit)
\par\smallskip\noindent\textbf{Lean statement (printed by Lean; verbatim):}
\begin{verbatim}
@DMC.Paper.V_le_U_of_stopping : ∀ {Ω : Type u_1} {Θ : Type u_2} [inst : MeasurableSpace Ω] [inst_1 : MeasurableSpace Θ]
  {H : ℕ → Type u_3} (mH : (t : ℕ) → MeasurableSpace (H t)) (R Rtrunc : (t : ℕ) → (Ω → H t) → Ω → Set ℝ)
  (Pcond : ℕ → MeasureTheory.ProbabilityMeasure Θ → MeasureTheory.Measure Ω)
  (C : ℕ → MeasureTheory.ProbabilityMeasure Θ → ℝ) (𝒢 : DMC.Paper.Richness Ω),
  (∀ (t : ℕ) (μ : MeasureTheory.ProbabilityMeasure Θ),
      DMC.Paper.U mH R Pcond 𝒢 t μ = max (DMC.Paper.V mH Rtrunc Pcond 𝒢 t μ) (C t μ)) →
    ∀ (t : ℕ) (μ : MeasureTheory.ProbabilityMeasure Θ), DMC.Paper.V mH Rtrunc Pcond 𝒢 t μ ≤ DMC.Paper.U mH R Pcond 𝒢 t μ
\end{verbatim}
% END-LEAN-STMT
\end{faithcheck}

\begin{faithcheck}[\texttt{DateValues} is now \emph{derived}, not postulated]\label{fc:datevalues-derived}
    \lean{DMC.Paper.toDateValues, DMC.Paper.toDateValues_U}\leanok
    \uses{def:datevalues, fc:ubdd, fc:vnonneg, fc:vleu-stop}
    \texttt{toDateValues} builds a \texttt{DateValues} from the paper's definitions: \texttt{U} is
    the defined $U_t^{\mathcal G}$ (\texttt{toDateValues\_U}, definitionally), \texttt{U\_bddAbove}
    comes from Assumption~2(iv) and \texttt{V\_nonneg} from the no-sale primitive. Everything
    downstream (\S3, including the belief-space no-gain theorem) consumes \texttt{DateValues}
    unchanged --- but the instance it consumes is now derived from the paper's $U_t$.

% BEGIN-LEAN-STMT (auto-generated by tools/gen_lean_statements.py -- do not edit)
\par\smallskip\noindent\textbf{Lean statements (printed by Lean; verbatim):}
\begin{verbatim}
@DMC.Paper.toDateValues : {Ω : Type u_1} →
  {Θ : Type u_2} →
    [inst : MeasurableSpace Ω] →
      [inst_1 : MeasurableSpace Θ] →
        {H : ℕ → Type u_3} →
          (mH : (t : ℕ) → MeasurableSpace (H t)) →
            (R Rtrunc : (t : ℕ) → (Ω → H t) → Ω → Set ℝ) →
              (Pcond : ℕ → MeasureTheory.ProbabilityMeasure Θ → MeasureTheory.Measure Ω) →
                [∀ (t : ℕ), Nonempty (H t)] →
                  [∀ (t : ℕ) (μ : MeasureTheory.ProbabilityMeasure Θ), MeasureTheory.IsProbabilityMeasure (Pcond t μ)] →
                    {M : ℝ} →
                      (∀ (t : ℕ) (Γ : Ω → H t) (ω : Ω), ∀ r ∈ R t Γ ω, r ≤ M) →
                        (∀ (t : ℕ) (Γ : Ω → H t) (ω : Ω), ∀ r ∈ Rtrunc t Γ ω, r ≤ M) →
                          (∀ (t : ℕ) (Γ : Ω → H t) (μ : MeasureTheory.ProbabilityMeasure Θ),
                              ∃ ρ ∈ DMC.Paper.Sel R t Γ, MeasureTheory.Integrable ρ (Pcond t μ)) →
                            (∀ (t : ℕ) (Γ : Ω → H t) (ω : Ω), 0 ∈ Rtrunc t Γ ω) →
                              (𝒢 : DMC.Paper.Richness Ω) →
                                (∀ (t : ℕ) (μ : MeasureTheory.ProbabilityMeasure Θ),
                                    DMC.Paper.V mH Rtrunc Pcond 𝒢 t μ ≤ DMC.Paper.U mH R Pcond 𝒢 t μ) →
                                  DMC.DateValues Θ
\end{verbatim}
\begin{verbatim}
@DMC.Paper.toDateValues_U : ∀ {Ω : Type u_1} {Θ : Type u_2} [inst : MeasurableSpace Ω] [inst_1 : MeasurableSpace Θ]
  {H : ℕ → Type u_3} (mH : (t : ℕ) → MeasurableSpace (H t)) (R Rtrunc : (t : ℕ) → (Ω → H t) → Ω → Set ℝ)
  (Pcond : ℕ → MeasureTheory.ProbabilityMeasure Θ → MeasureTheory.Measure Ω) [inst_2 : ∀ (t : ℕ), Nonempty (H t)]
  [inst_3 : ∀ (t : ℕ) (μ : MeasureTheory.ProbabilityMeasure Θ), MeasureTheory.IsProbabilityMeasure (Pcond t μ)] {M : ℝ}
  (hb : ∀ (t : ℕ) (Γ : Ω → H t) (ω : Ω), ∀ r ∈ R t Γ ω, r ≤ M)
  (hbV : ∀ (t : ℕ) (Γ : Ω → H t) (ω : Ω), ∀ r ∈ Rtrunc t Γ ω, r ≤ M)
  (hsel :
    ∀ (t : ℕ) (Γ : Ω → H t) (μ : MeasureTheory.ProbabilityMeasure Θ),
      ∃ ρ ∈ DMC.Paper.Sel R t Γ, MeasureTheory.Integrable ρ (Pcond t μ))
  (hzero : ∀ (t : ℕ) (Γ : Ω → H t) (ω : Ω), 0 ∈ Rtrunc t Γ ω) (𝒢 : DMC.Paper.Richness Ω)
  (hVU :
    ∀ (t : ℕ) (μ : MeasureTheory.ProbabilityMeasure Θ),
      DMC.Paper.V mH Rtrunc Pcond 𝒢 t μ ≤ DMC.Paper.U mH R Pcond 𝒢 t μ)
  (t : ℕ) (μ : MeasureTheory.ProbabilityMeasure Θ),
  (DMC.Paper.toDateValues mH R Rtrunc Pcond hb hbV hsel hzero 𝒢 hVU).U t μ = DMC.Paper.U mH R Pcond 𝒢 t μ
\end{verbatim}
% END-LEAN-STMT
\end{faithcheck}

\begin{faithcheck}[Collapse is exactly the reverse inequality]\label{fc:collapse-iff}
    \lean{DMC.Paper.valueCollapse_iff}\leanok
    \uses{def:collapse, fc:umono}
    Since the full-history value always dominates (Faithfulness check~\ref{fc:umono}), collapse
    holds iff the richer value is also $\le$ the coarser one.

% BEGIN-LEAN-STMT (auto-generated by tools/gen_lean_statements.py -- do not edit)
\par\smallskip\noindent\textbf{Lean statement (printed by Lean; verbatim):}
\begin{verbatim}
@DMC.Paper.valueCollapse_iff : ∀ {Ω : Type u_1} {Θ : Type u_2} [inst : MeasurableSpace Ω] [inst_1 : MeasurableSpace Θ]
  {H : ℕ → Type u_3} (mH : (t : ℕ) → MeasurableSpace (H t)) (R : (t : ℕ) → (Ω → H t) → Ω → Set ℝ)
  (Pcond : ℕ → MeasureTheory.ProbabilityMeasure Θ → MeasureTheory.Measure Ω) {𝓨 : Type u_4} [inst_2 : MeasurableSpace 𝓨]
  [∀ (t : ℕ), Nonempty (H t)]
  [∀ (t : ℕ) (μ : MeasureTheory.ProbabilityMeasure Θ), MeasureTheory.IsProbabilityMeasure (Pcond t μ)] {M : ℝ},
  (∀ (t : ℕ) (Γ : Ω → H t) (ω : Ω), ∀ r ∈ R t Γ ω, r ≤ M) →
    (∀ (t : ℕ) (Γ : Ω → H t) (μ : MeasureTheory.ProbabilityMeasure Θ),
        ∃ ρ ∈ DMC.Paper.Sel R t Γ, MeasureTheory.Integrable ρ (Pcond t μ)) →
      ∀ {S : Ω → MeasureTheory.ProbabilityMeasure Θ} {Y : Ω → 𝓨} {𝒢 : DMC.Paper.Richness Ω},
        DMC.Paper.AdmissibleRichness S Y 𝒢 →
          ∀ (t : ℕ),
            DMC.Paper.ValueCollapse mH R Pcond S Y 𝒢 t ↔
              ∀ (μ : MeasureTheory.ProbabilityMeasure Θ),
                DMC.Paper.U mH R Pcond { σ := MeasurableSpace.comap (fun ω => (S ω, Y ω)) inferInstance } t μ ≤
                  DMC.Paper.U mH R Pcond 𝒢 t μ
\end{verbatim}
% END-LEAN-STMT
\end{faithcheck}

%======================================================================
% Faithfulness audit (Meta/ModelFaithfulness.lean): definitions -> green facts
%======================================================================
\begin{faithcheck}[The axioms are consistent (non-vacuous)]\label{fc:consistent}
    \lean{DMC.singleShot_consistent}\leanok
    \uses{def:singleshot}
    The \texttt{SingleShot} axiom bundle (\texttt{V\_nonneg}, \texttt{V\_le\_U},
    \texttt{U\_bddAbove}, \texttt{condExp\_mono}, \texttt{affine\_harmonic}, \texttt{stopping}) is
    \textbf{jointly satisfiable}: a concrete witness over the genuine compact Polish space
    \texttt{unitInterval} (\texttt{DMC.frozenBeliefWitness}) type-checks. Hence the \S3 theorems
    that quantify over \texttt{SingleShot} are not vacuously true.

% BEGIN-LEAN-STMT (auto-generated by tools/gen_lean_statements.py -- do not edit)
\par\smallskip\noindent\textbf{Lean statement (printed by Lean; verbatim):}
\begin{verbatim}
DMC.singleShot_consistent : Nonempty (DMC.SingleShot ↑unitInterval)
\end{verbatim}
% END-LEAN-STMT
\end{faithcheck}

\begin{faithcheck}[Deferral is a real option ($V<U$)]\label{fc:vltu}
    \lean{DMC.witness_V_lt_U}\leanok
    \uses{fc:consistent}
    In the witness, $V_t(\mu)<U_t(\mu)$ strictly at every date and belief. So \texttt{V\_le\_U} is
    a genuine inequality (deferral strictly available) and is not a hidden $V=U$ collapse.

% BEGIN-LEAN-STMT (auto-generated by tools/gen_lean_statements.py -- do not edit)
\par\smallskip\noindent\textbf{Lean statement (printed by Lean; verbatim):}
\begin{verbatim}
DMC.witness_V_lt_U : ∀ (t : ℕ) (μ : MeasureTheory.ProbabilityMeasure ↑unitInterval),
  DMC.frozenBeliefWitness.V t μ < DMC.frozenBeliefWitness.U t μ
\end{verbatim}
% END-LEAN-STMT
\end{faithcheck}

\begin{faithcheck}[The $\max$ encodes stop-vs.-continue]\label{fc:stop}
    \lean{DMC.SingleShot.stop_iff}\leanok
    \uses{def:singleshot}
    ``Allocate now'' ($U_t=V_t$) holds \emph{iff} deferral is (weakly) worse than allocating. So
    the $\max$ in \texttt{stopping} transparently encodes the stopping decision.

% BEGIN-LEAN-STMT (auto-generated by tools/gen_lean_statements.py -- do not edit)
\par\smallskip\noindent\textbf{Lean statement (printed by Lean; verbatim):}
\begin{verbatim}
@DMC.SingleShot.stop_iff : ∀ {Θ : Type u_1} [inst : TopologicalSpace Θ] [inst_1 : MeasurableSpace Θ]
  (D : DMC.SingleShot Θ) (t : ℕ) (μ : MeasureTheory.ProbabilityMeasure Θ),
  D.U t μ = D.V t μ ↔ D.condExp (D.U (t + 1)) t μ ≤ D.V t μ
\end{verbatim}
% END-LEAN-STMT
\end{faithcheck}

\begin{faithcheck}[Bayes-plausibility fixes constants]\label{fc:condexp-const}
    \lean{DMC.SingleShot.condExp_const}\leanok
    \uses{def:singleshot}
    A constant belief functional is harmonic --- its expected next value is itself. This is the
    $\lambda\equiv c$ case of \texttt{affine\_harmonic}: total probability is conserved across
    dates, confirming the operator is a genuine (belief-)martingale expectation.

% BEGIN-LEAN-STMT (auto-generated by tools/gen_lean_statements.py -- do not edit)
\par\smallskip\noindent\textbf{Lean statement (printed by Lean; verbatim):}
\begin{verbatim}
@DMC.SingleShot.condExp_const : ∀ {Θ : Type u_1} [inst : TopologicalSpace Θ] [inst_1 : MeasurableSpace Θ]
  (D : DMC.SingleShot Θ) (c : ℝ) (t : ℕ) (μ : MeasureTheory.ProbabilityMeasure Θ), D.condExp (fun x => c) t μ = c
\end{verbatim}
% END-LEAN-STMT
\end{faithcheck}

\begin{faithcheck}[The envelope is transparently $\sup_t U_t$]\label{fc:envelope}
    \lean{DMC.DateValues.envelope_eq_iSup}\leanok
    \uses{def:envelope}
    \texttt{DateValues.envelope} unfolds definitionally to $\sup_t U_t$, matching the paper's $g$.

% BEGIN-LEAN-STMT (auto-generated by tools/gen_lean_statements.py -- do not edit)
\par\smallskip\noindent\textbf{Lean statement (printed by Lean; verbatim):}
\begin{verbatim}
@DMC.DateValues.envelope_eq_iSup : ∀ {Θ : Type u_1} [inst : MeasurableSpace Θ] (D : DMC.DateValues Θ)
  (μ : MeasureTheory.ProbabilityMeasure Θ), D.envelope μ = ⨆ t, D.U t μ
\end{verbatim}
% END-LEAN-STMT
\end{faithcheck}
